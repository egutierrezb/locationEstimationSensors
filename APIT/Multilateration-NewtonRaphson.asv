%Possible refinement for APIT

%To create positions of the anchors
sumx=0;
sumy=0;
for i=1:5
    A(i,:)=[10*rand(1,1) 10*rand(1,1)]; 
    sumx=sumx+A(i,1);
    sumy=sumy+A(i,2);
end

%Compute the initial position of the node, the centroid is a good
%approximatioN
S=[sumx/5 sumy/5];


